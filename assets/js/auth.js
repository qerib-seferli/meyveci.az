import { $, supabase, toast, formData } from './core.js';
import { initLayout } from './layout.js';
document.addEventListener('DOMContentLoaded',()=>{initLayout(); const page=document.body.dataset.page; if(page==='login') initLogin(); if(page==='register') initRegister(); if(page==='forgot') initForgot(); if(page==='reset') initReset();});
function initLogin(){ $('#loginForm')?.addEventListener('submit',async e=>{e.preventDefault(); const f=formData(e.target); const {error}=await supabase.auth.signInWithPassword({email:f.email,password:f.password}); if(error) return toast(error.message==='Email not confirmed'?'Email təsdiqlənməyib':error.message); const {data}=await supabase.from('profiles').select('role').eq('email',f.email).maybeSingle(); if(data?.role==='admin') location.href='./admin/index.html'; else if(data?.role==='courier') location.href='./courier/index.html'; else location.href='./index.html';});}
function initRegister(){ $('#registerForm')?.addEventListener('submit',async e=>{e.preventDefault(); const f=formData(e.target); if(f.password!==f.password2) return toast('Şifrələr eyni deyil'); const {error}=await supabase.auth.signUp({email:f.email,password:f.password,options:{data:{first_name:f.first_name,last_name:f.last_name,phone:f.phone}}}); if(error) return toast(error.message); toast('Qeydiyyat tamamlandı. Giriş edə bilərsiniz.'); setTimeout(()=>location.href='./login.html',900);});}
function initForgot(){ $('#forgotForm')?.addEventListener('submit',async e=>{e.preventDefault(); const f=formData(e.target); const {error}=await supabase.auth.resetPasswordForEmail(f.email,{redirectTo:location.origin+location.pathname.replace('forgot-password.html','reset-password.html')}); if(error) return toast(error.message); toast('Şifrə yeniləmə linki göndərildi');});}
function initReset(){ $('#resetForm')?.addEventListener('submit',async e=>{e.preventDefault(); const f=formData(e.target); if(f.password!==f.password2) return toast('Şifrələr eyni deyil'); const {error}=await supabase.auth.updateUser({password:f.password}); if(error) return toast(error.message); toast('Şifrə yeniləndi'); setTimeout(()=>location.href='./login.html',900);});}
